@model ExploreBooks.Models.UserViewModels.IndexViewModel
@inject IUserHistoryService BookService

@{
    ViewData["Title"] = "Profile";
}

@{
    foreach (var activity in Model.BookActivity)
    {
        var book = BookService.ConvertFromBookStateToBook(activity.TargetId);
        <h3>@activity.DateModified.Date</h3>
        if (@book != null)
        {
            <h4>@book.Title</h4>
        }
    }
}

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
