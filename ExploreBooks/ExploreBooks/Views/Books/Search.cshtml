@model IEnumerable<Book>

@{
    ViewData["Title"] = "Index";
}

<environment include="Development">
    <link rel="stylesheet" href="~/css/Books/Book.css" />
</environment>

<br />
<br />
<div class="row justify-content-center">
    <h3>
        Browse through all searched books
    </h3>
</div>
<br />
<br />
<div class="container">
    @{
        if (Model != null)
        {
            var books = Model.ToList();
            <div class="row">
                @for (var index = 0; index < books.Count; ++index)
                {
                    var checkIndex = index + 1;
                    var displayInfo = "<h4>" + books[index].Title + "</h4><p>" + books[index].Description + "</p>";

                    <div class="col-md-3 col-3">
                        <a asp-action="Details" asp-route-id="@books[index].Id" data-toggle="tooltip" data-placement="right" data-html="true" title="@displayInfo">
                            <img class="rounded" id="bookImage" src="@Url.Content("~/images/" + books[index].Folder + "/" + books[index].ImageName)" />
                        </a>
                    </div>

                    if (checkIndex % 4 == 0)
                    {
                        <div class="w-100"></div>
                        <br />
                    }
                }
            </div>
        }
        else
        {
            <h5>
                There is no book containing these letters!
            </h5>
        }
    }
</div>